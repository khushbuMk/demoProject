import React from 'react';
import { View, Text } from 'react-native';

const graph = () => {
    return (
        <View>
            <Text>Search</Text>
        </View>
    );
};

export default graph;