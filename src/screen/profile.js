import React from 'react';
import { View, Text } from 'react-native';

const profile = () => {
    return (
        <View>
            <Text>Search</Text>
        </View>
    );
};

export default profile;